NODE_ENV=production
PORT=3000

CORS_ORIGIN=http://localhost:3001

# Optional: protect dashboard with a password
# DASHBOARD_PASSWORD=your_password_here

# Optional: require auth on all API endpoints (useful when exposing on LAN)
# REQUIRE_AUTH=true
